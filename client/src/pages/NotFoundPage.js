/**
 * Regresa al tutorial {@tutorial first-tutorial}
 * @module client/src/pages/NotFoundPage
 */
import React from "react";
/**
 * Retorna que la dirección url no ha sido encontrada si la dirección es incorrecta o no existe
 * @returns {Object}
 */
export function NotFoundPage(){
    return(
        <div>Not Found Page</div>
    )
}